export type Booking = {
  id: string
  userId: string
  eventId: string
  eventTitle: string
  eventImage: string
  eventStart: string
  eventEnd: string
  venueName: string
  status: string
  totalPrice: number
  currency: string
  seats: string[]
  createdAt: string
  updatedAt: string
}
